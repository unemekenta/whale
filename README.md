# README
## 初期設定
以下の内容のenvファイルをルート直下に作成する。(******は任意の設定値)
```
RAILS_ENV=******

API_HOST=******
API_HOST_PORT=******
API_CONTAINER_PORT=******
DB_HOST_PORT=******
DB_CONTAINER_PORT=******

DB_NAME=******
DB_USER=******
DB_PASSWORD=******
```
